# Coding Challenge for Safeguard

---

## Requirements

Clone the repository and install it by running:
`npm i`

### Run headless tests

`npx cypress run`

### Run the tests using UI

`npx cypress open`
